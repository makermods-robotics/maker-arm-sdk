# Diagnostics

These tools are optional and are not installed as public commands. `encoder_gui.py` and
`bench_backend.py` are torque-free. `jog_one_motor.py` applies torque and must be used only with
the arm supported and its documented motion bound understood.
